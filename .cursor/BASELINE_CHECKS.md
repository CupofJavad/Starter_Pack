# Known-Good Baselines

Python:
- python --version works
- pytest --version works
- ruff --version works
- pyright --version works

Node (if used):
- node --version works
- pnpm --version works
- pnpm install succeeds on clean clone

Network:
- HTTPS outbound works
- DNS resolution works

If any baseline fails, fix environment first (do not debug app logic yet).
