# Active Assumptions

- Python >= 3.11 available
- Internet access usually available during development
- Ubuntu server has outbound HTTPS and DNS resolution
- SQLite is acceptable for local prototyping
- Credentials are loaded via env vars (never hardcoded)
- No paid services unless explicitly approved

If an assumption becomes false, update this file.
